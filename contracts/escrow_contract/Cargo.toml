[package]
name = "skillescrow-escrow-contract"
version = "0.2.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20.5.0"

[dev-dependencies]
soroban-sdk = { version = "20.5.0", features = ["testutils"] }

[patch.crates-io]
ethnum = { git = "https://github.com/nrayburn-tech/ethnum-rs", tag = "v1.5.3" }
